<div class="flashes">
    {% set flashes = { 'warning': 'alert-warning', 'notice': 'alert-info', 'error': 'alert-danger', 'success': 'alert-success' } %}

    {% for key, messages in app.session.getFlashBag().all() %}
        {% for message in messages %}
            <div class="alert {{ (flashes[key] is defined)?flashes[key]:'alert-info' }}">
                <button type="button" class="close" data-dismiss="alert">×</button>
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}
</div>
