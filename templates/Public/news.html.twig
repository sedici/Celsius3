{% extends 'Public/public.html.twig' %}

{% block title %}
    {{ 'Home'|trans }}
{% endblock %}

{% block content %}

    <h2>{{ 'News'|trans }}</h2>
    <hr class="separator_hr">
    <div class="navigation">
        {{ knp_pagination_render(pagination) }}
    </div>
    {% for news in pagination %}
        {% include 'Public/_news.html.twig' %}
        {% include 'Public/_news_modal.html.twig' %}
    {% endfor %}
    <div class="navigation">
        {{ knp_pagination_render(pagination) }}
    </div>
{% endblock %}
