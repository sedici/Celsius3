{% extends 'Order/_index_element.html.twig' %}

{% block admin_data %}
    <div class="busquedas">
        {{ 'searches'|trans }}: {{ count_searches(request) }}
    </div>
    {% for event in request.events %}
        {% if (event.getEventType() == 'cancel' and event.getCancelledByUser()) %}
            <div>
                {{ 'cancelledByUser' | trans }}
            </div>
        {% endif %}
    {% endfor %}
{% endblock %}

{% block admin %}
    <div class="order-operator">
        <a href="#">{{ request.operator }}</a>
    </div>

    <div class="order-actions">
        <ul class="list-unstyled actions">
            <li>
                <a href="{{ path('admin_order_edit', { 'id': element.id }) }}"><span class="glyphicon glyphicon-pencil"></span></a>
            </li>
        </ul>
    </div>
{% endblock %}
