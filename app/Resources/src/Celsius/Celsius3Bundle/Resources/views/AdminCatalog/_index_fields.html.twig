<div class="sorting-group pull-right">
    <span>
        {{ 'Sort by'|trans }}: 
    </span>
    {% include 'CelsiusCelsius3Bundle:AdminCatalog:_index_sort.html.twig' %}
</div>
<div class="clearfix"></div>
{% for catalog in pagination %}
<div id="id{{ catalog.id }}" class="accordion">
    <div class="accordion-group">
        <div class="accordion-heading">
            <a href="#collapse{{ catalog.id }}" data-parent="#id{{ catalog.id }}" data-toggle="collapse" class="accordion-toggle">
                <span>{{ catalog.name }}</span>
            </a>
            {% include 'CelsiusCelsius3Bundle:AdminCatalog:_index_list_actions.html.twig' %}
        </div>
        <div class="accordion-body collapse" id="collapse{{ catalog.id }}">
            <div class="accordion-inner">
                {% include 'CelsiusCelsius3Bundle:AdminCatalog:_show_fields.html.twig' %}
            </div>
        </div>
    </div>
</div>
{% endfor %}