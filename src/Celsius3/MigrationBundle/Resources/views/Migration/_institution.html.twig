'{{ institution.name }}' => array(
    'abbreviation' => '{{ institution.abbreviation }}',
    'website' => '{{ institution.website }}',
    'address' => '{{ institution.address }}',
    'institutions' => array(
{% for institution2 in institutions %}
{{ render_institution(institution2)|raw }}
{% endfor %}
    ),
),
