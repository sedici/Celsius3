{{ form_start(form, { 'action': path('fos_user_profile_edit'), 'method': 'POST', 'class': 'form', 'role': 'form' }) }}
    {{ form_widget(form) }}
<button type="submit" class="btn btn-primary submit-button"><span
            class="glyphicon glyphicon-ok"></span> {{ 'profile.edit.submit'|trans({}, 'FOSUserBundle') }}</button>
<a href="{{ path('fos_user_change_password') }}">{{ 'Change password'|trans }}</a>
{{ form_end(form) }}
