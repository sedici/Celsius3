<div class="sorting-group pull-right">
    <span>
        {{ 'Sort by'|trans }}: 
    </span>
    {% include 'Celsius3CoreBundle:SuperadminContactType:_index_sort.html.twig' %}
</div>
<div class="clearfix"></div>
{% for contacttype in pagination %}
<div id="id{{ contacttype.id }}" class="accordion">
    <div class="accordion-group">
        <div class="accordion-heading">
            <a href="#collapse{{ contacttype.id }}" data-parent="#id{{ contacttype.id }}" data-toggle="collapse" class="accordion-toggle">
                <span>{{ contacttype.name }}</span>
            </a>
            {% include 'Celsius3CoreBundle:SuperadminContactType:_index_list_actions.html.twig' %}
        </div>
    </div>
</div>
{% endfor %}