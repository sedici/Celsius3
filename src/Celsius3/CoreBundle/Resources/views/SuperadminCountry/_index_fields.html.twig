<div class="sorting-group pull-right">
    <span>
        {{ 'Sort by'|trans }}: 
    </span>
    {% include 'Celsius3CoreBundle:SuperadminCountry:_index_sort.html.twig' %}
</div>
<div class="clearfix"></div>
{% include 'Celsius3CoreBundle:Layout:_batch.html.twig' %}
<form class="batch_form" action="{{ path('superadmin_country_batch') }}" method="post">
    <input type="hidden" id="batch-action" name="action" value=""></input>
    {% for element in pagination %}
    <div id="id{{ element.id }}" class="accordion">
        <div class="accordion-group">
            <div class="accordion-heading">
                <input type="checkbox" name="element[]" id="element{{ element.id }}" value="{{ element.id }}" class="pull-left batch-checkbox"/>
                <a href="#collapse{{ element.id }}" data-parent="#id{{ element.id }}" data-toggle="collapse" class="accordion-toggle">
                    <span>{{ element.name }}</span>
                </a>
                {% include 'Celsius3CoreBundle:SuperadminCountry:_index_list_actions.html.twig' %}
            </div>
            <div class="accordion-body collapse" id="collapse{{ element.id }}">
                <div class="accordion-inner">
                    {% include 'Celsius3CoreBundle:SuperadminCountry:_show_fields.html.twig' %}
                </div>
            </div>
        </div>
    </div>
    {% endfor %}
</form>