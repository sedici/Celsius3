<form action="{{ path('fos_user_resetting_reset', { 'url': app.request.get('url'), 'token': token }) }}" {{ form_enctype(form) }} method="POST" class="fos_user_resetting_reset form">
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary submit-button"><span class="glyphicon glyphicon-ok"></span> {{ 'resetting.reset.submit'|trans({}, 'FOSUserBundle') }}</button>
</form>
