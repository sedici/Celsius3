<div class="sorting-group pull-right">
    <span>
        {{ 'Sort by'|trans }}: 
    </span>
    {% include 'Celsius3CoreBundle:SuperadminCatalog:_index_sort.html.twig' %}
</div>
<div class="clearfix"></div>
{% include 'Celsius3CoreBundle:Layout:_batch.html.twig' %}
<form class="batch_form" action="{{ path('superadmin_catalog_batch') }}" method="post">
    <input type="hidden" id="batch-action" name="action" value=""></input>
    {% for catalog in pagination %}
    <div id="id{{ catalog.id }}" class="accordion">
        <div class="accordion-group">
            <div class="accordion-heading">
                <input type="checkbox" name="element[]" id="element{{ catalog.id }}" value="{{ catalog.id }}" class="pull-left batch-checkbox"/>
                <a href="#collapse{{ catalog.id }}" data-parent="#id{{ catalog.id }}" data-toggle="collapse" class="accordion-toggle">
                    <span>{{ catalog.name }}</span>
                </a>
                {% include 'Celsius3CoreBundle:SuperadminCatalog:_index_list_actions.html.twig' %}
            </div>
            <div class="accordion-body collapse" id="collapse{{ catalog.id }}">
                <div class="accordion-inner">
                    {% include 'Celsius3CoreBundle:SuperadminCatalog:_show_fields.html.twig' %}
                </div>
            </div>
        </div>
    </div>
    {% endfor %}
</form>