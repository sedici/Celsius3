<h1>Location list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Postalcode</th>
            <th>Institutions</th>
            <th>Country</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for document in documents %}
        <tr>
            <td><a href="{{ path('location_show', { 'id': document.id }) }}">{{ document.id }}</a></td>
            <td>{{ document.name }}</td>
            <td>{{ document.postalCode }}</td>
            <td>{{ document.institutions }}</td>
            <td>{{ document.country }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('location_show', { 'id': document.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('location_edit', { 'id': document.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('location_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
