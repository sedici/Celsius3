<table class="table table-striped table-bordered table-condensed records_list">
    <thead>
        <tr>
            <th>{{ pagination.sortable('Name'|trans, 'name')|raw }}</th>
            <th>{{ pagination.sortable('Surname'|trans, 'surname')|raw }}</th>
            <th>{{ pagination.sortable('Birthdate'|trans, 'birthdate')|raw }}</th>
            <th>{{ pagination.sortable('Username'|trans, 'username')|raw }}</th>
            <th>{{ pagination.sortable('Email'|trans, 'email')|raw }}</th>
            <th>{{ 'Actions'|trans }}</th>
        </tr>
    </thead>
    <tbody>
    {% for document in pagination %}
        <tr>
            <td>{{ document.name }}</td>
            <td>{{ document.surname }}</td>
            <td>{% if document.birthdate %}{{ document.birthdate|date('d-m-Y') }}{% endif%}</td>
            <td>{{ document.username }}</td>
            <td>{{ document.email }}</td>
            <td>
                <ul class="unstyled">
                    <li>
                        <a href="{{ path('user_show', { 'code': app.request.get('code'), 'id': document.id }) }}"><i class="icon-book"></i> {{ 'Show'|trans }}</a>
                    </li>
                    <li>
                        <a href="{{ path('user_edit', { 'code': app.request.get('code'), 'id': document.id }) }}"><i class="icon-pencil"></i> {{ 'Edit'|trans }}</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>