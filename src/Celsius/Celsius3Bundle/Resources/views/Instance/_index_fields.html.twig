<table class="table table-striped table-bordered table-condensed records_list">
    <thead>
        <tr>
            <th>{{ pagination.sortable('Code'|trans, 'code')|raw }}</th>
            <th>{{ pagination.sortable('Name'|trans, 'name')|raw }}</th>
            <th>{{ pagination.sortable('Website'|trans, 'website')|raw }}</th>
            <th>{{ pagination.sortable('Title'|trans, 'title')|raw }}</th>
            <th>{{ 'Actions'|trans }}</th>
        </tr>
    </thead>
    <tbody>
    {% for document in pagination %}
        <tr>
            <td>{{ document.code }}</td>
            <td>{{ document.name }}</td>
            <td>{{ document.website }}</td>
            <td>{{ document.title }}</td>
            <td>
                <ul class="unstyled">
                    <li>
                        <a href="{{ path('instance_show', { 'id': document.id }) }}"><i class="icon-book"></i> {{ 'Show'|trans }}</a>
                    </li>
                    <li>
                        <a href="{{ path('instance_edit', { 'id': document.id }) }}"><i class="icon-pencil"></i> {{ 'Edit'|trans }}</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>