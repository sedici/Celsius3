{% extends 'CelsiusCelsius3Bundle:Layout:layout.html.twig' %}

{% block title %}
{{ 'Search Results'|trans }}
{% endblock %}

{% block content %}
<h2>{{ 'Search Results'|trans }}</h2>
<div class="container-fluid">
    <div class="row-fluid">
        <div class="span9">
            <h3>{{ 'Order'|trans }}</h3>
            <div class="navigation">
                {{ pagination.render()|raw }}
            </div>
            <table class="table table-striped table-bordered table-condensed records_list">
                {% include 'CelsiusCelsius3Bundle:Order:_index_fields.html.twig' %}
            </table>
            <div class="navigation">
                {{ pagination.render()|raw }}
            </div>
        </div>
        <div class="span3">
            {% from "CelsiusCelsius3Bundle:Macro:crud.html.twig" import admin_search as admin_search %}
            {{ admin_search(app.request.get('keyword')) }}
        </div>
    </div>
</div>
{% endblock %}