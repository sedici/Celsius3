<div id="{{ 'info_'~state }}" class="collapse {{ isCurrent?'in':'' }}">
    <div class="well">
        <p>{{ 'Date'|trans }}: {{ order.hasState(constant('Celsius\\Celsius3Bundle\\Manager\\StateManager::STATE__CREATED'), instance)?order.getState(constant('Celsius\\Celsius3Bundle\\Manager\\StateManager::STATE__CREATED'), instance).date|localizeddate():'' }}</p>
    </div>
</div>