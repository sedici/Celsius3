<a class="{{ order.hasState(state)?'':'disabled' }} {{ hasPrevious?'pointer':'' }}" data-toggle="{{ hasPrevious?'collapse':'' }}" data-parent="#state-info" href="#{{ 'info_'~state~(order.hasState(state)?'':'_disabled') }}">
    {{ state|replace({ '_': ' ' })|capitalize|trans }}
</a>