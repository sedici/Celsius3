<div id="{{ 'info_'~state }}" class="collapse {{ isCurrent?'in':'' }}">
    <div class="well">
        <p>{{ 'Date'|trans }}: {{ order.hasState('delivered', instance)?order.getState('delivered', instance).date|localizeddate():'' }}</p>
    </div>
</div>