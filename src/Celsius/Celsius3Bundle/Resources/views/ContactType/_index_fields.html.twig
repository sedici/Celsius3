<table class="table table-striped table-bordered table-condensed records_list">
    <thead>
        <tr>
            <th>{{ pagination.sortable('Name'|trans, 'name')|raw }}</th>
            <th>{{ 'Actions'|trans }}</th>
        </tr>
    </thead>
    <tbody>
    {% for document in pagination %}
        <tr>
            <td>{{ document.name }}</td>
            <td>
                <ul class="unstyled">
                    <li>
                        <a href="{{ path('contacttype_show', { 'id': document.id }) }}"><i class="icon-book"></i> {{ 'Show'|trans }}</a>
                    </li>
                    <li>
                        <a href="{{ path('contacttype_edit', { 'id': document.id }) }}"><i class="icon-pencil"></i> {{ 'Edit'|trans }}</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>